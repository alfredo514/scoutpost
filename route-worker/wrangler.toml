#:schema node_modules/wrangler/config-schema.json
# Serves the site Worker at softsauce.co/scoutpost without touching the
# existing origin. Delete this Worker entirely if Scoutpost moves to its own
# domain — then attach the domain to that Worker and set BASE_PATH="/".
name = "scoutpost-route"
main = "src/index.js"
compatibility_date = "2026-08-01"

# The site Worker this proxies to.
[vars]
PAGES_ORIGIN = "https://scoutpost.scoutpost.workers.dev"

# Requires softsauce.co to be an active zone on this Cloudflare account.
[[routes]]
pattern = "softsauce.co/scoutpost*"
zone_name = "softsauce.co"
